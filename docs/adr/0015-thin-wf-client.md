# Moved: ADR 0048, Ship `wf` as a thin workspace client

This legacy path is retained so existing public links do not break. The decision was renumbered because it shared ADR 0015 with an earlier, unrelated decision.

Redirect: [ADR 0048: Ship `wf` as a thin workspace client](0048-thin-wf-client.md)

The canonical ADR 0015 remains [HTTP Trigger와 외부 HTTP Route Binding을 분리한다](0015-provider-neutral-http-route-bindings.md).
